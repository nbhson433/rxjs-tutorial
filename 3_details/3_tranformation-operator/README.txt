map(): thay đổi dữ liệu trước khi được subscribe
  
delay(ms): sau ms mới emit data

pluck(): lấy property của object

reduce(): sử dụng như reduce trong js

toArray(): chuyển data thành mảng

buffer($envet) - bufferTime(ms): Khi không muốn bỏ qua giá trị khi subscribe, nhưng cần phải có hiệu lệnh thì mới emit data